# bm

open my bookmarks from terminal

## usage

```
	Usage
	  $ bm [input]

	Options
	  --in <app name>
		--incognito

	Examples
	  $ bm lambda
	  $ bm ec2 --in firefox --incognito
	  $ bm dynamo --in "google chrome"
```
